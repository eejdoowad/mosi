{
  "name": "counter",
  "description": "simple counter example for mosi",
  "scripts": {
    "build": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "mosi": "0.0.2",
    "rollup": "^0.39.2"
  }
}
