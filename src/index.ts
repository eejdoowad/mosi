import {
  con as cs_con,
  id as cs_id,
  init as cs_init,
  net as cs_net
} from "./cs";

export const cs = {
  con: cs_con,
  id: cs_id,
  init: cs_init,
  net: cs_net
};

import {
  con as bp_con,
  id as bp_id,
  init as bp_init,
  net as bp_net
} from "./bp";

export const bp = {
  con: bp_con,
  id: bp_id,
  init: bp_init,
  net: bp_net
};
